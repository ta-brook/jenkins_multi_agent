# Docker-plugin clouds: one per always-on Docker host (the project's "workers").
# Each cloud carries the SAME four env-labeled agent templates.
# Env boundary = the label; a template's label is unique to one environment.
# Images come from env so the harness (local DinD) and real hosts can differ.
# NOTE: templates bind-mount the Docker socket and run as root so pipeline
# build/deploy steps can use the host daemon; containers are ephemeral.
jenkins:
  clouds:
    - docker:
        name: "docker-host-a"
        dockerApi:
          dockerHost:
            uri: "${DOCKER_HOST_A_URI}"
            credentialsId: "docker-host-a"
          connectTimeout: 30
          readTimeout: 60
        containerCap: 10
        templates:
          - name: "docker-build"
            labelString: "docker-build"
            mode: EXCLUSIVE
            instanceCap: 2
            remoteFs: "/home/jenkins/agent"
            pullStrategy: PULL_LATEST
            pullTimeout: 300
            dockerTemplateBase:
              image: "${AGENT_IMAGE_TOOL_BUILD}"
              user: "root"
              mounts:
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"
              memoryLimit: 2048
              cpus: "1.0"
            connector:
              jnlp:
                jenkinsUrl: "${JENKINS_URL}"
          - name: "docker-dev"
            labelString: "docker-dev"
            mode: EXCLUSIVE
            instanceCap: 2
            remoteFs: "/home/jenkins/agent"
            pullStrategy: PULL_LATEST
            pullTimeout: 300
            dockerTemplateBase:
              image: "${AGENT_IMAGE_DEV}"
              user: "root"
              mounts:
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"
              memoryLimit: 2048
              cpus: "1.0"
            connector:
              jnlp:
                jenkinsUrl: "${JENKINS_URL}"
          - name: "docker-staging"
            labelString: "docker-staging"
            mode: EXCLUSIVE
            instanceCap: 2
            remoteFs: "/home/jenkins/agent"
            pullStrategy: PULL_LATEST
            pullTimeout: 300
            dockerTemplateBase:
              image: "${AGENT_IMAGE_STAGING}"
              user: "root"
              mounts:
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"
              memoryLimit: 2048
              cpus: "1.0"
            connector:
              jnlp:
                jenkinsUrl: "${JENKINS_URL}"
          - name: "docker-prod"
            labelString: "docker-prod"
            mode: EXCLUSIVE
            instanceCap: 1
            remoteFs: "/home/jenkins/agent"
            pullStrategy: PULL_LATEST
            pullTimeout: 300
            dockerTemplateBase:
              image: "${AGENT_IMAGE_PROD}"
              user: "root"
              mounts:
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"
              memoryLimit: 2048
              cpus: "1.0"
            connector:
              jnlp:
                jenkinsUrl: "${JENKINS_URL}"

    - docker:
        name: "docker-host-b"
        dockerApi:
          dockerHost:
            uri: "${DOCKER_HOST_B_URI}"
            credentialsId: "docker-host-b"
          connectTimeout: 30
          readTimeout: 60
        containerCap: 10
        templates:
          - name: "docker-build"
            labelString: "docker-build"
            mode: EXCLUSIVE
            instanceCap: 2
            remoteFs: "/home/jenkins/agent"
            pullStrategy: PULL_LATEST
            pullTimeout: 300
            dockerTemplateBase:
              image: "${AGENT_IMAGE_TOOL_BUILD}"
              user: "root"
              mounts:
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"
              memoryLimit: 2048
              cpus: "1.0"
            connector:
              jnlp:
                jenkinsUrl: "${JENKINS_URL}"
          - name: "docker-dev"
            labelString: "docker-dev"
            mode: EXCLUSIVE
            instanceCap: 2
            remoteFs: "/home/jenkins/agent"
            pullStrategy: PULL_LATEST
            pullTimeout: 300
            dockerTemplateBase:
              image: "${AGENT_IMAGE_DEV}"
              user: "root"
              mounts:
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"
              memoryLimit: 2048
              cpus: "1.0"
            connector:
              jnlp:
                jenkinsUrl: "${JENKINS_URL}"
          - name: "docker-staging"
            labelString: "docker-staging"
            mode: EXCLUSIVE
            instanceCap: 2
            remoteFs: "/home/jenkins/agent"
            pullStrategy: PULL_LATEST
            pullTimeout: 300
            dockerTemplateBase:
              image: "${AGENT_IMAGE_STAGING}"
              user: "root"
              mounts:
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"
              memoryLimit: 2048
              cpus: "1.0"
            connector:
              jnlp:
                jenkinsUrl: "${JENKINS_URL}"
          - name: "docker-prod"
            labelString: "docker-prod"
            mode: EXCLUSIVE
            instanceCap: 1
            remoteFs: "/home/jenkins/agent"
            pullStrategy: PULL_LATEST
            pullTimeout: 300
            dockerTemplateBase:
              image: "${AGENT_IMAGE_PROD}"
              user: "root"
              mounts:
                - "type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"
              memoryLimit: 2048
              cpus: "1.0"
            connector:
              jnlp:
                jenkinsUrl: "${JENKINS_URL}"
